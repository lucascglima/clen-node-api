{
  "name": "clen-node-api",
  "version": "1.0.0",
  "description": "API Node utilizando clean architecture",
  "main": "index.js",
  "scripts": {
    "test": "jest --watchAll --silent --verbose --colors --noStackTrace",
    "test:unit": "npm test -- --watch -c jest-unit-config.js",
    "test:staged": "jest --passWithNotests --findRelatedTests --silent",
    "test:ci": "npm test -- --coverage"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lucascglima/clen-node-api.git"
  },
  "author": "Lucas Lima",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/lucascglima/clen-node-api/issues"
  },
  "homepage": "https://github.com/lucascglima/clen-node-api#readme",
  "devDependencies": {
    "husky": "^7.0.4",
    "jest": "^27.4.0",
    "lint-staged": "^12.1.2",
    "standard": "^16.0.4"
  },
  "dependencies": {
    "validator": "^13.7.0"
  }
}
